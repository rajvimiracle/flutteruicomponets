import 'package:flutter/material.dart';

class SildableListPage extends StatelessWidget {
  const SildableListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
