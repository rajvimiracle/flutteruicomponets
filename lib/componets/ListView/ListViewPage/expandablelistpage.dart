import 'package:flutter/material.dart';

class ExpadableListPage extends StatelessWidget {
  const ExpadableListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
